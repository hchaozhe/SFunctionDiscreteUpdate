# SFunctionDiscreteUpdate
Show the difference between having discrete update in different location.
Note that this would make the output have the same step size as the default ones.

Verified in MATLAB 2016a
